version: "3.9"
services:
  planer_frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: planer-frontend:latest
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    ports:
      - "3001:3000"
    restart: unless-stopped
